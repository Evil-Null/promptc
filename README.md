# Prompt Compiler

Universal CLI Prompt Compiler — elite prompt engineering, automatically.

## Install

```bash
pip install -e .
```

## Usage

```bash
mycli --help
mycli version
mycli health
mycli health --check config_valid
mycli health --strict
```

## Development

```bash
pip install -e ".[dev]"
pytest
```
